# 薄氷 設計土台

## 1. 薄氷は何か

薄氷は、学園アニメ的な部活動の流れを通して、持ち込まれた依頼を部としての結論へ変え、その過程と学びを蓄積していくシステムである。

技術的には、次の3つを同時に成立させる。

- 会議が進む
- 課題が解ける
- 学びが残る

## 2. 基本原則

1. 部として結論を出すことが価値
2. 熱は依頼の解釈と設計で生まれる
3. 役割は固定職ではなく、前に出やすい局面
4. 全員が毎回前に出る必要はない
5. 関係は固定ペアではなく、起こりやすい偏り
6. 台本っぽくしない

## 3. Why / How / What

薄氷は、Why を入口に、How を会議で形にし、What を結論として出す。

- `Why`
  依頼者の背景、動機、何が気になっているか
- `How`
  部としてどう進めるか。論点、懸念、案、進め方、分解、見極め
- `What`
  最終的に何を作るか。部としての結論、成果物、提出内容

### 役割との対応

- える: Why を最初に受ける
- ハルヒ: How の方向を熱で押す
- キョン: How の無理を止める
- 折木: How を最小案に落とす
- 長門: How が技術的に成立するかを見極める
- える: What を部の結論として言葉にする

## 4. 学校モデル

Obsidian は学校そのものとして扱う。
場所による記憶と共通イメージで、複雑な情報構造を直感的に扱えるようにする。

### 4.1 場所

- `部室`
  正式な依頼が持ち込まれ、部活が始まり、結論が出る場所
- `黒板`
  部室の中で、今の状態を見ながら会議を進めるための共有面
- `活動記録`
  部室で起きた流れを残す日誌
- `図書館/開架`
  外部由来の雑多な知識
- `図書館/薄氷バックナンバー`
  薄氷の中で育った再利用可能なやり方
- `掲示板`
  未整理なネタ、思いつき、チラシ、改善案の置き場
- `職員室`
  薄氷そのものを運営するための資料置き場

補足:

- `掲示板` は主線の依頼受付ではない
- 正式依頼は `request.sh` から `room_requests.yaml` に入る
- `掲示板` は一次情報の保管場所として独立している

### 4.2 Obsidian 対応

- `薄氷/部室/黒板.md`
- `薄氷/部室/活動記録/`
- `薄氷/図書館/開架/`
- `薄氷/図書館/薄氷バックナンバー/`
- `薄氷/掲示板/`
- `薄氷/職員室/`

## 5. 情報の流れ

1. 正式な依頼はユーザーが部室に持ち込む
2. えるが依頼を受け、背景や why を軽くヒアリングする
3. えるが背景を要約して部室に共有する
4. 依頼と背景を黒板に書く
5. 会議しながら黒板が更新される
6. 完結時に黒板の最終状態を活動記録へ転写する
7. 必要なら活動記録からバックナンバーを編む
8. さらに何度も効いたものだけを skill / instruction に昇格する

### 5.1 正式依頼の状態遷移

- `room_requests.yaml` が正式依頼の入口
- `noticeboard` / 掲示板 は正式依頼の入口ではない
- 受付直後は `new`
- えるが `request_id` を引き継いで部会を作ると `in_progress`
- えるが結論を提出すると `done`

## 6. 黒板

黒板は、現在進行中の依頼についての人間向け共有面である。
読み物ではなく、会議を進めるための現在値。

### 6.1 黒板の性質

- 常に1枚だけ存在する
- 依頼が完結したら消す
- 途中状態を含んでよい
- 追記、修正、斜線、消去が前提
- えるが前に立って更新するが、中身は部全体の議論を反映する

### 6.2 黒板に出す項目

- 依頼
- 背景
- 論点
- 懸念
- タスク（担当つき）
- 決まったこと
- 保留
- 完了
- 今の部としての結論

### 6.3 表現

- 未処理: 通常表示
- 完了: `[x]` または取り消し線
- 保留: 明示的に残す
- 不要: 消す、または却下として扱う

## 7. 活動記録

活動記録は、その日の部活の流れを残す日誌である。
1つの依頼が完結するまでの流れを記録する。

### 7.1 活動記録の役割

- 黒板がどう変化したかを残す
- 依頼の背景がどう効いたかを残す
- どこで熱が出て、どこで止まったかを残す
- 最終的にどう結論へ至ったかを残す

### 7.2 黒板との違い

- 黒板: 会議を進めるための現在値
- 活動記録: 会議がどう進んだかを残す履歴

### 7.3 保存タイミング

- えるが提出を完了した時に自動生成する
- 入力は `gijiroku.yaml` と黒板の最終状態、必要なら各部員の報告
- 保存先は `activity-log/` と `薄氷/部室/活動記録/`

## 7.4 完了後の扱い

- 活動記録を保存したら、その依頼の会議状態は閉じる
- `gijiroku.yaml` は `meeting: null` に戻す
- 黒板は初期状態に戻す
- つまり、黒板は常に「今まさに進行中の依頼」だけを表示する

## 8. バックナンバー

バックナンバーは、活動記録から抽出された、再利用したいやり方を残す本棚である。
活動記録の圧縮版ではない。

### 8.1 目的

似た依頼が来たときに、「あの時のやり方」を取りに行けるようにする。
つまり、薄氷のやり方を育てるために存在する。

### 8.2 作成条件

- 毎回作るものではない
- ハルヒが面白いと感じる
- えるが次に使えると判断する
- 両方そろった時だけバックナンバー化する

### 8.3 単位

- 一回の部活ごとではなく、得られたやり方ごと
- 少し抽象化されたパターンとして残す

### 8.4 形式

- タイトル
- どういう時に使うか
- やり方
- 気をつけること
- 面白かった理由
- 元になった活動記録

### 8.5 作り方

- 活動記録から直接編む
- 自動では作らない
- `面白い` と `次に使える` が両方そろった時だけ作る
- 保存先は `archive/` と `薄氷/図書館/薄氷バックナンバー/`

## 9. スキル / instruction への昇格

スキルや instruction は、バックナンバーの中でも特に何度も効いたものを、実行ルールとして固定したもの。

### 9.1 昇格の流れ

- 活動記録: 具体的な流れ
- バックナンバー: 少し抽象化されたやり方
- スキル / instruction: 実行ルールへ昇格したもの

### 9.2 昇格時の変化

- 面白かった理由は薄まる
- 実行条件や手順が強まる

## 10. 内部状態

内部状態は、黒板・活動記録・バックナンバーを作るための構造化された現在値。
黒板そのものではなく、その裏にある会議の地図。

### 10.1 内部状態の最低項目

- `why.request`
- `why.background`
- `why.motivation`
- `why.requester_intent`
- `why.success_signal`
- `how.topics`
- `how.concerns`
- `how.options`
- `how.approach`
- `how.task_breakdown`
- `how.assignments`
- `how.feasibility`
- `how.decisions`
- `how.pending`
- `how.done`
- `what.conclusion`
- `what.rationale`
- `what.deliverables`
- `what.submission`
- `revision`

### 10.2 原則

- 5W1H を帳票のように全部埋める必要はない
- える中心で更新する
- 必要なら他の部員の言葉も反映する
- 議論と進行の中で育っていく柔らかい地図として持つ

### 10.3 内部状態の束ね方

- `why`
  - `request`
  - `background`
  - `motivation`
  - `requester_intent`
  - `success_signal`
- `how`
  - `topics`
  - `concerns`
  - `options`
  - `approach`
  - `task_breakdown`
  - `assignments`
  - `feasibility`
  - `decisions`
  - `pending`
  - `done`
- `what`
  - `conclusion`
  - `rationale`
  - `deliverables`
  - `submission`
- `revision`
  - `requested`
  - `note`
  - `requested_changes`
  - `minimal_fix`
  - `reality_check`
  - `feasibility`

### 10.4 黒板への投影

内部状態をそのまま全部黒板に出す必要はない。
黒板には、今の会議に必要な現在値だけを投影する。

- `why.request` → `依頼`
- `why.background` → `背景`
- `how.topics` → `論点`
- `how.concerns` → `懸念`
- `how.assignments` / `how.task_breakdown` → `タスク`
- `how.decisions` → `決まったこと`
- `how.pending` → `保留`
- `how.done` → `完了`
- `what.conclusion` → `今の部としての結論`

## 11. 実装方針

- 会議状態は構造化して持つ
- 黒板はその人間向け表示
- 活動記録は黒板の変化を含む履歴
- バックナンバーは活動記録から後で編む
- 役割は固定しすぎず、前に出やすい局面だけを制約として持つ
